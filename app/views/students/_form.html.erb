<%= form_for(student) do |f| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
        <%= f.label :name, "Ім'я"%>
        <%= f.text_field :name,class:"form-control" %>

        <%= f.label :birthday, "День народження" %>
        <%= f.date_select :birthday,class:"form-control" %>

        <%= f.label :education, "Навчальний заклад" %>
        <%= f.text_field :education,class:"form-control" %>
    <% if current_user.admin?%>
        <%= f.label :activated, "Активований"%>
        <%= f.check_box :activated,class:"form-control" %>

        <%= f.label :group_id, "Група" %>
        <%= f.text_field :group_id,class:"form-control" %>
    <%end%>
        <%= f.submit "Підтвердити", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
