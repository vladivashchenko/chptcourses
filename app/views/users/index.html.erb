<% provide(:title, 'Всі користувачі') %>
<h1>Всі користувачі</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
